/**
 * constants/api.ts — Demo stub.
 *
 * The app is fully frontend-only. API_BASE_URL is kept so that any remaining
 * import sites don't break at compile time, but it is never used at runtime.
 */
export const API_BASE_URL = "";

